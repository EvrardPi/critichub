default:
  controller: Main
  action: home

media:
  controller: Main
  action: media

login:
  controller: Auth
  action: login

logout:
  controller: Auth
  action: logout

register:
  controller: Auth
  action: register

confirm:
  controller: Auth
  action: confirmAccount

forgot:
  controller: Auth
  action: viewForgotPwd

forgot-confirmation:
  controller: Auth
  action: forgotPassword

reset-password:
  controller: Auth
  action: viewResetPassword

confirm-reset-password:
  controller: Auth
  action: resetPassword

404:
  controller: Error
  action: error404

403:
  controller: Error
  action: error403

500:
  controller: Error
  action: error500

401:
  controller: Error
  action: error401

410:
  controller: Error
  action: error410


  
create-review:
  controller: Cms
  action: userReview

new-admin-review:
  controller: Cms
  action: adminReview

a-propos:
  controller: Main
  action: aboutUs

about-us:
  controller: Main
  action: aboutUs

contact:
  controller: Main
  action: contact

back-read-user:
  controller: Users
  action: readUser

back-create-user:
  controller: Users
  action: createUser

back-delete-user:
  controller: Users
  action: deleteUser

back-update-user:
  controller: Users
  action: updateUser

back-get-user:
  controller: Users
  action: getUser

back-view-user:
  controller: Users
  action: view

back-view-category:
  controller: Categorys
  action: view

back-create-category:
  controller: Categorys
  action: createCategory

back-read-category:
  controller: Categorys
  action: readCategory

back-delete-category:
  controller: Categorys
  action: deleteCategory

back-update-category:
  controller: Categorys
  action: updateCategory

back-get-category:
  controller: Categorys
  action: getCategory

back-view-gestionfront:
  controller: GestionFronts
  action: view

back-create-gestionfront:
  controller: GestionFronts
  action: createContent

back-get-content:
  controller: GestionFronts
  action: getContent

back-gestion-background:
  controller: GestionFronts
  action: createBackground